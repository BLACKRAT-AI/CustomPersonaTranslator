<v:SettingsPage x:Class="CPT.Shell.Views.CloningSetupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:v="clr-namespace:CPT.Shell.Views"
        Background="{StaticResource Bg}"
        Foreground="{StaticResource Text}"
        FontFamily="Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="SemiBold" FontSize="14"
                   Text="Voice cloning setup (Chatterbox)" />

        <TextBlock Grid.Row="1" Margin="0,8,0,0" TextWrapping="Wrap">
            One-time install. This downloads a portable Python (~30 MB), torch + chatterbox-tts
            (~200 MB CPU / ~2 GB CUDA), and on first use will pull the ~3 GB Chatterbox model.
            Nothing leaves your machine.
        </TextBlock>

        <ProgressBar Grid.Row="2" x:Name="Bar" Margin="0,16,0,0" Height="6"
                     IsIndeterminate="False"
                     Background="{StaticResource FieldBg}"
                     Foreground="{StaticResource Text}"
                     BorderBrush="{StaticResource BorderBr}"/>

        <Border Grid.Row="3" Margin="0,12,0,0"
                Background="{StaticResource FieldBg}" BorderBrush="{StaticResource BorderBr}" BorderThickness="1">
            <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="Log" FontFamily="Consolas" FontSize="11"
                           Foreground="{StaticResource Muted}" Padding="8" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="StartBtn"  Content="Start setup" Click="OnStart" FontWeight="SemiBold" />
            <Button x:Name="CloseBtn"  Content="Close"       Click="OnClose" />
        </StackPanel>
    </Grid>
</v:SettingsPage>
