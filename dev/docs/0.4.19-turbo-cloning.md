# Turbo cloning, 0.4.19

User auditioned Turbo against Original from the same C3PO selected reference, preferred Turbo (including its longer sample), then requested all personas be updated.

Added persisted per-persona CloneModel selection (Original/Turbo) in the clone editor and preview/save paths. Existing JSON defaults Original; explicitly updated all five installed personas with valid references to Turbo. Default persona has no cloned reference and was not changed. Reference hashes stayed unchanged. Turbo uses the auditioned generation defaults (temperature .8, top_p .95, top_k 1000), with the unsupported Original delivery control disabled. Switching model happens inside the serialized clone-worker gate; only one model worker remains loaded. Startup warmup now runs while standby is listening. Turbo skips obsolete stock acknowledgement cache generation.

The packaged server loads cached Turbo weights without online metadata checks and uses a Windows cache copy workaround instead of requiring symlink privileges. Original remains selectable; no automatic voice/model substitution occurs.

Checks: 345 .NET tests passed; fake-model silence-retry tests passed; actual WPF UI smoke passed. Actual Python server synthesized all five installed references successfully and verified unchanged source SHA256. Warm/new-reference generation: C3PO first request 9.19s, Gamma 3.94s, Jarvis 2.58s, Computer 2.31s, VIKI 2.16s. Server cold load was 36.72s in this run. Earlier audition: Turbo 1.86s/3.14s for short/long samples, Original 6.56s/104.09s. Single-run values, not latency guarantees or automatic voice-likeness validation.

Actual AppServices desktop test confirmed Turbo selection and final audio playback at 43.3s after a cold request, answer at 23.7s. The desktop action itself FAILED (entered incorrect text); retain this as a failed overall test, not a passed computer-control test. No claim that this change fixes agent task accuracy.

Published and copied self-contained exe and server to both installed and previously stale Debug launch locations. Installed exe SHA256 matches published exe. Backups preserve previous exe, server and persona JSON. Listening gallery: LocalAppData/CustomPersonaTranslator/voice-auditions/turbo-all-personas/listen.html.
