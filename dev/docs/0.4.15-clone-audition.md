# C3PO reference investigation

The September 8 19:28 clone saved youtube-Z_OjTojCNm0-2.wav. Its 1,621,952 PCM bytes exactly match concatenating the four selected, millisecond-rounded source intervals. Duration: 50.686 seconds. Full source: 200.7945625 seconds. The latest and earlier selected WAV files are identical. The engine log selects the new WAV; it is not conditioning on the full video.

This establishes correct extraction and routing, not voice likeness. No automatic change to the saved voice or reference was made. Three generated auditions compare current settings, native generation defaults and the second selected clip alone. They are listening comparisons, not passed similarity tests. A model-quality limitation remains possible.

The persona editor now plays the exact reference via Listen to reference, alongside Preview voice. Both previews can be stopped. Changing source, leaving the editor, or starting an agent turn closes reference playback. WAV duration is read from its actual format. The Delivery label no longer promises faithful cloning.

Build and UI smoke checks passed. The installed source-playback button was exercised and the running editor inspected visually. Original references and persona settings were retained.
