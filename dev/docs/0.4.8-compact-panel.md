# 0.4.8

Restored the Computer persona's original youtube-1ZXugicgn6U.wav sample path. The original, -2, and -3 samples have identical SHA-256 hashes; none was deleted. Previous persona configuration was backed up alongside its JSON file. Actual synthesis using the original Computer sample produced nonzero PCM in 35.8 seconds; this verifies synthesis, not perceptual voice quality.

Ordinary edits to existing personas now save directly when the voice source and writing samples are unchanged. Color, image, name, description, expression and output preferences preserve the learned prompt and cloned voice. These saves do not regenerate the persona, prepare the clone or speak a confirmation.

The floating panel now consists of the compact voice-control bar and optional avatar. Conversation header, history, composer, new-conversation and chat expansion controls are removed. Web avatar transcripts are disabled. Settings, agent/persona selection, trigger phrases, push-to-talk, standby, volume, stop, pin and avatar controls remain.

Validation: clean solution build; UI smoke tests including direct color-save preservation of the Computer clone and compact-bar control checks passed. Actual installed window visually inspected: one compact bar, required voice controls present, conversation controls absent. Self-contained installed executable version 0.4.8 matches published SHA-256 DEF0E898B8F6D376C1BC3243914C4B112B7268374A0BE80C9794AF8840FF0094. Prior executable and settings backed up before replacement.
